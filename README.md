## Usage

### Installation

Install the dependencies:

```bash
npm install
```

### Development

Start the development server and visit [http://localhost:5173](http://localhost:5173):

```bash
npm run dev
```

### Build

To build the app for production, run:

```bash
npm run build
```

The generated files will be in the `dist` folder, ready to be served.
